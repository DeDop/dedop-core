name: dedop-docs
channels:
    - conda-forge
    - defaults
dependencies:
  - python=3.6*
  - cate-util
  - mock
  #
  # for testing
  #
  - pytest
  - pytest-cov
  - tornado
  - sphinx-argparse
  - pip:
    - sphinx_autodoc_annotation
